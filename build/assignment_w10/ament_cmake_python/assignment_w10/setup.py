from setuptools import find_packages
from setuptools import setup

setup(
    name='assignment_w10',
    version='0.0.0',
    packages=find_packages(
        include=('assignment_w10', 'assignment_w10.*')),
)
